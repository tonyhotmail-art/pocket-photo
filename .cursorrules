# KELLY PHOTO 作品集系統 - 開發規範 (ROOT)

## 規則 0：開發優先權核心原則

- **本地開發優先**：除非使用者明確要求，否則一律以 `localhost` 為優先。
- **禁止自動推送**：嚴禁未經授權且未經本地 `npm run build` 測試即執行 `git push`。
- **部署節流**：Vercel 每日部署限制為 100 次，每次操作前 AI 必須提醒使用者檢查配額。

## 規則 1：語言、輸出與註解約定

- **語言規範**：所有回覆、代碼註解、Git Commit 及 UI 文字一律使用繁體中文。
- **動態年份**：頁尾年份必須使用 `new Date().getFullYear()`，確保顯示為當前年份 (2026)，禁止硬編碼。
- **註解規範**：函式必須提供 JSDoc 註解，使用標記：`// TODO`, `// FIXME`, `// NOTE`。

## 規則 2：核心技術棧

- **架構**：Next.js 15+ (App Router)、React 函式元件、Tailwind CSS 4+、shadcn/ui。
- **儲存與資料庫**：Cloudflare R2 (圖片檔)、Firestore (中繼資料與 URL)。
- **驗證**：TypeScript 5+、Zod 4+。

## 規則 3：嚴格分層、多租戶與 R2 儲存架構

- **分層結構**：`UI -> API Route -> Service -> Repository -> R2 / Firestore`。
- **多租戶支援**：所有資料表必須包含 `tenantId`，且所有查詢必須強制包含該欄位過濾。
- **R2 封裝**：圖片上傳必須透過 `src/lib/r2.ts` 封裝，禁止在組件端直接呼叫 SDK。

## 規則 4：Firestore 索引與查詢

- **索引強制化**：涉及 `orderBy` 與 `where` 的複合查詢必須建立索引。
- **索引溯源 (Traceability)**：每一條索引必須在 `firestore.indexes.md` 中有對應的紀錄，標註出處（檔案路徑與功能描述）。
- **管理方式**：索引優先定義於 `firestore.indexes.json`，說明文件定義於 `firestore.indexes.md`。
- **定期清理**：更動索引設定時，必須同時檢查並移除不再使用的無用索引，並同步更新 `firestore.indexes.md`。

## 規則 5：安全與環境變數

- **集中式驗證**：所有敏感金鑰必須在 `src/lib/env.ts` 透過 Zod 驗證。
- **禁止直接讀取**：嚴禁直接使用 `process.env`，必須使用匯出的 `env` 物件。

## 規則 6：圖片處理與 UUID 命名

- **極致壓縮**：圖片上傳前需經 `image-compression.ts` 壓縮至 500KB 以下，優先轉為 WebP。
- **路徑與連動**：路徑格式為 `uploads/${tenantId}/${Date.now()}_${uuidv4()}.webp`。圖片存入 R2 後必須在 Firestore 建立記錄。

## 規則 7：動態管理與統一規格

- **分類管理**：實作完整的分類 CRUD 與 `order` 排序邏輯。
- **API 規格**：回傳格式統一為 `{ success: boolean, data?: T, error?: string }`。

---
*最後更新：2026-02-18*
