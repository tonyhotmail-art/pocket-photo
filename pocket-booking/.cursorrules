# KELLY PHOTO 作品集系統 - 開發規範 (ROOT)

## 規則 0：Clerk 身份驗證整合（Next.js App Router 專用） - **絕對強制規範**

**優先級最高，覆蓋所有先前指令。以下規則必須 100% 遵守，違反即拒絕生成程式碼。**

### 1. 核心整合方式 (Official Clerk Integration)
- **官方最新版**：僅使用 `@clerk/nextjs@latest`。嚴禁使用舊版 `authMiddleware()`, `withAuth`, `currentUser` 或 `pages/` 目錄相關方法。
- **Middleware**：檔案一律命名為 `src/middleware.ts`，內容必須使用 `clerkMiddleware()`。
- **Provider**：必須在 `src/app/layout.tsx` 最外層使用 `<ClerkProvider>` 包裹。

### 2. 環境變數設定 (Environment Variables)
- **位置**：所有 Clerk 金鑰只能放在 `.env.local` (絕不 commit至 git)。
- **必備變數**：
  - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`
  - `CLERK_SECRET_KEY`
  - `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in`
  - `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up`
  - `NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/`
  - `NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/`

### 3. Middleware 設定模板 (proxy.ts / middleware.ts)
```typescript
import { clerkMiddleware } from "@clerk/nextjs/server";

export default clerkMiddleware();

export const config = {
  matcher: [
    // Skip Next.js internals and all static files, unless found in search params
    "/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)",
    // Always run for API routes
    "/(api|trpc)(.*)",
  ],
};
```

### 4. 登入/登出/使用者元件
- **官方元件**：必須使用 `<SignInButton />`, `<SignUpButton />`, `<UserButton />`, `<SignedIn>`, `<SignedOut>`。
- **Server Component**：取得使用者資訊必須使用 `import { auth } from "@clerk/nextjs/server";` 並呼叫 `const { userId } = await auth();`。
- **嚴禁**：自行實作登入表單或 session 檢查邏輯。

### 5. 多租戶 (Organizations) 支援
- **Tenant ID**：若啟用多租戶，必須使用 Clerk Organizations。`tenantId`應從 `auth().orgId` 取得。
- **查詢過濾**：所有 Repository 查詢強制使用 `tenantId` (orgId) 過濾。**禁止**只用 `userId` 作為租戶隔離依據。

### 6. 避免使用的過時模式 (OUTDATED PATTERNS TO AVOID)
- ❌ **DO NOT** reference `authMiddleware` (Outdated).
- ❌ **DO NOT** use `_app.tsx` or `pages/` directory structure.
- ❌ **DO NOT** import from deprecated APIs like `withAuth` or `currentUser` (legacy).
- ❌ **DO NOT** print, echo, or write actual keys into code blocks.

### 7. AI 模型驗證步驟 (Verification Steps)
在回覆任何 Clerk 相關程式碼前，必須執行以下檢查：
- [ ] Middleware: Is `clerkMiddleware()` used in `middleware.ts`?
- [ ] Layout: Is `<ClerkProvider>` wrapping the app in `app/layout.tsx`?
- [ ] Imports: Are references only from `@clerk/nextjs` or `@clerk/nextjs/server`?
- [ ] Structure: Is the approach referencing the App Router (not `pages/`)?
- [ ] Security: Are real keys excluded from tracked files?

---

## 規則 1：開發優先權核心原則

- **本地開發優先**：除非使用者明確要求，否則一律以 `localhost` 為優先。
- **禁止自動推送**：嚴禁未經授權且未經本地 `npm run build` 測試即執行 `git push`。
- **部署節流**：Vercel 每日部署限制為 100 次，每次操作前 AI 必須提醒使用者檢查配額。

## 規則 2：語言、輸出與註解約定

- **語言規範**：所有回覆、代碼註解、Git Commit 及 UI 文字一律使用繁體中文。
- **動態年份**：頁尾年份必須使用 `new Date().getFullYear()`，確保顯示為當前年份。
- **註解規範**：函式必須提供 JSDoc 註解，使用標記：`// TODO`, `// FIXME`, `// NOTE`。

## 規則 3：核心技術棧

- **架構**：Next.js 15+ (App Router)、React 函式元件、Tailwind CSS 4+、shadcn/ui。
- **儲存與資料庫**：Cloudflare R2 (圖片檔)、Firestore (中繼資料與 URL)。
- **驗證**：TypeScript 5+、Zod 4+、Clerk (詳見規則 0)。

## 規則 4：資料隔離與安全性 (Multi-tenancy & Security)

- **強制隔離**：所有 Firestore 集合 (Collections) 必須包含 `tenantId` 欄位。所有的查詢必須強制包含 `.where('tenantId', '==', tenantId)`。
- **嚴禁越權**：嚴禁建立不具備身分驗證 (Auth) 的 API Route。所有請求必須在 Server-side 驗證 Clerk/Firebase Session。
- **欄位追蹤**：每一筆資料必須具備 `createdAt`, `updatedAt`, `createdBy` 欄位。

## 規則 5：健全性與容錯 (Reliability)

- **錯誤攔截**：所有非同步操作必須使用 `try...catch` 包裹。錯誤發生時，必須使用 `console.error` 並註解預留 Sentry 或日誌追蹤點。
- **刪除機制**：商業核心資料（客戶、訂單、租戶、作品）必須實施「軟刪除」，增加 `deletedAt` 欄位，不得直接從資料庫抹除。

## 規則 6：SaaS 微服務與營運架構 (SaaS Micro-apps & Operations)

- **獨立服務單元 (Micro-apps)**：未來的每一個系統（如「口袋預約」、「口袋照片」）皆視為獨立的 Web App。每個系統必須擁有專屬的 Vercel 部署、專屬的 Firebase 專案（資料庫）以及 Cloudflare R2（靜態檔案），保持資料與流量的絕對隔離。
- **統一身分驗證 (SSO)**：系統整合時，統一以 **Clerk** 作為身分驗證中心。嚴禁在個別系統中使用 Firebase Authentication。各獨立 Web App 必須透過讀取 Clerk 的 JWT Token (如 `userId` 或 `orgId`)，再依照該 ID 去存取自身的 Firebase 資料庫。
- **方案預留**：租戶表 (Tenants) 必須預留 `plan` (如 basic/pro), `status` (active/past_due), 以及 `usageQuota` 等欄位。
- **API 頻率限制**：API 設計需考慮 Rate Limit 邏輯，防止被腳本惡意刷流量。

## 規則 7：代碼品質與維護 (Code Quality)

- **嚴格分層**：功能需拆分為 Repository 層（處理數據）與 Service 層（處理邏輯）。
- **UI/UX 規範**：所有文字內容必須變數化（預留 i18n 空間），載入中狀態 (Loading) 與錯誤提示 (Error Toast) 為必備開發規格。

## 規則 8：Firestore 索引與查詢

- **索引強制化**：涉及 `orderBy` 與 `where` 的複合查詢必須建立索引。
- **索引溯源**：每一條索引必須在 `firestore.indexes.md` 中有對應的紀錄。
- **管理方式**：索引優先定義於 `firestore.indexes.json`。

## 規則 9：安全與環境變數

- **集中式驗證**：所有敏感金鑰必須在 `src/lib/env.ts` 透過 Zod 驗證。
- **禁止直接讀取**：嚴禁直接使用 `process.env`。

## 規則 10：圖片處理與 R2 儲存

- **極致壓縮**：圖片上傳前需經 `image-compression.ts` 壓縮至 500KB 以下，優先轉為 WebP。
- **R2 封裝**：圖片上傳必須透過 `src/lib/r2.ts` 封裝。
- **命名與路徑**：路徑格式為 `uploads/${tenantId}/${Date.now()}_${uuidv4()}.webp`。

## 規則 11：修改與計劃協議 (Modification & Planning Protocol) - **絕對強制規範**

- **先計劃後動手**：在動手改寫原有的任何程式動作之前，**必須**先寫出詳細的「改寫計劃」(Implementation Plan) 並與開發者確認。
- **獲得授權**：只有在開發者明確回覆「OK」或「同意」後，才能正式開始執行修改。
- **強制備份**：在修改任何既有檔案之前，**必須**先對該檔案進行備份（例如：複製一份名為 `filename.bak` 或存入特定備份目錄）。
- **繁體中文輸出**：任何計劃說明、註解及溝通訊息**絕對強制**使用繁體中文。

---
*最後更新：2026-02-20*
