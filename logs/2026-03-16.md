# 2026-03-16 開發日誌

## 任務 background
開發者回報線上版本相本突然消失，這是因為 Clerk 清空後，資料庫內的舊照片仍標記為過時的 `"kelly"` slug，導致頁面讀取不到。

## 修復細節
- 規劃統一性查表腳本：避免寫死姓名=ID，設計 `preview-migrate.js` 讀取 `global_tenants` 及 `admins` 以抓取動態關聯。
- 批次遷移：撰寫並執行 `execute-migrate.js`，將 Firestore 中的 357 筆 `portfolio_items` 與 6 筆 `categories` 的 `tenantId` 由 `"kelly"` 成功批次更正為 `"tenant_kelly_owner"`。
- 將 `src/actions/admin.ts` 與 `src/app/api/admin/sync-role/route.ts` 中修復的 Fallback 邏輯，以及最新的修復進度推送線上，觸發 Vercel 部署。

## 追加修復：首頁瀑布流載入異常

### 問題描述
開發者回報首頁的相片不再是一段一段載入的瀑布流。

### 原因與修復
1. **查詢排序干擾**：先前的程式碼在「全部作品」查詢中加入了 `orderBy('categoryOrder')` 與 `orderBy('categoryName')`，導致 Firebase 將所有相片依據分類群組化回傳，破壞了依 `createdAt` 降序排列的整體時間軸。
2. **客戶端過濾導致分頁斷鏈**：由於我們在客戶端會過濾掉回收區等隱藏分類，導致原本抓取數量可能低於 `displayLimit`，提早觸發了 `hasMore = false`。
3. **解決方案**：重寫 `[slug]/page.tsx` 的查詢邏輯，統一全域只用 `orderBy('createdAt', 'desc')` 提取相片，並改用「原始 Snapshot 數量」來精準判斷是否有下一頁（`hasNextPage`），徹底恢復了瀑布流的無限捲動體驗。
