# KELLYPHOTO 開發日誌 - 2026-03-14 (狀態暫停紀錄)

## 任務背景
- 啟動 Gemini CLI 並處理「口袋相片」作品集無法顯示照片的問題。
- 實作「藝廊邏輯」：路人可看公開照片，管理員可看全部。
## 目前現況與問題診斷 (Critical Findings)

### 1. 權限與索引狀態：
- **門衛規則**：已更新 `firestore.rules`，允許公開讀取非「待分類」照片。
- **索引定義**：已更新 `firestore.indexes.json` 以支援 `in` 與 `!=` 的複合查詢。

### 2. 資料斷層真相 (Final Discovery)：
- **網址 Slug**：目前使用的 URL 為 `kellyphoto`。
- **資料庫統計**：經 `count-ids.mjs` 統計，354 張照片的 `tenantId` 欄位皆為 `kelly`。
- **租戶登記**：`tenants` 集合中僅有 `DocID: tenant_kelly_owner` (其 `slug` 為 `kelly`)，完全找不到 `kellyphoto` 的紀錄。
- **結論**：照片顯示不出來是因為 `kellyphoto` (網址) 無法對應到 `kelly` (資料庫標籤)。

---
*狀態：開發者指示暫停所有動作，等待後續 ID 對齊處理。*

- **`GEMINI.md`**：整合 `.cursorrules` 核心規範（Clerk、資料隔離、計劃協議）。
- **`[slug]/page.tsx`**：實作雙認證 ID 解析與藝廊讀取邏輯（已修正 ReferenceError）。
- **`firestore.rules`**：調整為「公開藝廊」模式。
- **`firestore.indexes.json`**：新增複合索引定義。
- **`scripts/`**：建立 `test-gallery-logic.mjs` 與 `inspect-data.mjs` 等偵錯工具。

## 目前待辦事項 (Paused)
- [ ] **ID 對齊**：需決定是否在 `tenants` 補齊 `kellyphoto` 資料，或在前端做 Slug 映射。
- [ ] **索引驗證**：等待 Firestore 雲端索引生效或確認手動建立。
- [ ] **測試同步**：驗證管理員登入後是否能正確切換至「倉庫模式」。

---
*狀態：已依照開發者指令暫停，等待下一步指示。*
