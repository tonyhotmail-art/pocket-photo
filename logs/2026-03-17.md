# 2026-03-17 開發日誌

## 任務摘要
- **任務背景**：使用者詢問目前 GitHub 帳號上有多少專案。
- **修復細節 / 程式碼變動**：無。僅透過 GitHub CLI 查詢了 `@tonyhotmail-art` 的專案列表。
- **UI 變動**：無。
- **GitHub 推送紀錄**：無。
- **目前的待辦事項**：等待使用者的下一步指示。

## 任務摘要 (JULES LAB 推送)
- **任務背景**：使用者要求開一個名為 JULES LAB 的專案，將 POCKET-* 系列的專案複製進去後，推送到 GitHub 上並設定為私密。
- **修復細節 / 程式碼變動**：
  - 建立 `jules-lab` 資料夾。
  - 使用指令將 `pocket-admin`, `pocket-booking`, `pocket-city`, `pocket-shared-auth` 的內容複製到 `jules-lab` 內。
  - 自動排除過大的快取與依賴資料夾 (`node_modules`, `.next`, `.vercel`, `.git`, `dist`) 與私密環境變數 (`.env.local`)。
  - 於 `jules-lab` 中進行 `git init`，加入所有檔案並建立首次提交 `chore: initial commit from POCKET-* series projects`。
- **UI 變動**：無。
- **GitHub 推送紀錄**：已透過 GitHub CLI 建立 `https://github.com/tonyhotmail-art/jules-lab`，並推送 main 分支 (設定為 Private)。
- **目前的待辦事項**：已完成 JULES LAB 專案的建立與推送。等待使用者的下一步指示。

## 任務摘要 (Vercel Build Log 分析與修復)
- **任務背景**：使用者提供了一段 Vercel 的構建日誌 (Build Log) 詢問有哪些需要注意的地方，並指示修復其中的兩個警告。
- **修復細節 / 程式碼變動**：
  - **修復套件廢棄警告**：分析發現 `node-domexception@1.0.0` 是由 `node-fetch` 套件引入的，但全案 (`src/`) 並未使用到 `node-fetch`，因此透過指令 `npm uninstall node-fetch` 移除該無用依賴，解決此警告。
  - **修復 Next.js Middleware 警告**：配合 Next.js 16 (Turbopack) 棄用 `middleware` 命名，將 `portfolio/src/middleware.ts` 重新命名為 `portfolio/src/proxy.ts`。同時，也更新了根目錄的 `GEMINI.md` 開發規範，將 Clerk Middleware 的命名規定調整為 `src/proxy.ts`，以避免後續開發衝突。
- **UI 變動**：無。
- **GitHub 推送紀錄**：無，待使用者確認是否需推送。
- **目前的待辦事項**：警告已排除。等待使用者的下一步指示。